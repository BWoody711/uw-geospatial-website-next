/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/uw-geospatial-website-next",
    // output: "export",  // <=== enables static exports
    // reactStrictMode: true,
};

export default nextConfig;
